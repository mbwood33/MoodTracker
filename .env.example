# Copy to .env.local for machine-specific overrides.
# VITE_ variables are public and bundled into browser code. Never put secrets here.
VITE_APP_NAME=Mood Tracker
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
