# Feature modules

Each product capability will own its feature-specific presentation and
orchestration code here. A feature may depend on domain APIs and data-layer
interfaces, but shared layers must not depend back on a feature.

No feature logic exists in Phase 0.
